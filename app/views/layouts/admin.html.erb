<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title> <%= t('admin_panel') %> | Testguru2019</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'admin/nav'%>
    <%= render 'shared/flash'%>
    <div class="container">
      <ul class="nav">
        <li class="nav-item">
          <a class="nav-link active" href="<%= admin_root_path %>">Список тестов</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="<%= admin_gists_path %>">Гисты</a>
        </li>
      </ul>
        <%= yield %>  
      </div>    
  </body>
  
   <div class="navbar-fixed-bottom">
      <%= render 'shared/footer' %> 
  </div> 
</html>
